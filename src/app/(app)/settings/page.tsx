import { SettingsView } from "@/components/dashboard/views/SecondaryViews";

export const metadata = { title: "Settings — KareerTrack" };
export default function SettingsPage() { return <SettingsView />; }
